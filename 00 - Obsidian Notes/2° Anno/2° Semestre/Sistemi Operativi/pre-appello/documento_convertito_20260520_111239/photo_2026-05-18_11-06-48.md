## Pagina 1

```python
void *func2(void* param)
{
    pthread_mutex_lock(&mutex);
    var2 = var1 + 1; var1++;
    pthread_mutex_unlock(&mutex)
    return 0;
}

int main()
{
    pthread t_threads(NUM_THREADS);
    int pid;
    pid = fork();
    if (pid != 0) {
        pthread_create(&threads[0], NULL, func1, NULL);
        pthread_create(&threads[1], NULL, func2, NULL);
        pthread_join(threads[0], NULL);
        pthread_join(threads[1], NULL);
    }
    printf("%d %d\n", var1, var2);
    return 0;
}

4. Nel contesto dei metodi per eviare i deadlock:
    Data tre thread 70, 71, 72 e tre tipi di iscorza A (T iscorze), B (e iscorze), C (T iscorze) con le seguenti matrici di Allocazione e Max:
    Allocazione
    Max
    A B C
    A B C
    T0 2 1 1
    T1 2 3 2
    T2 1 1 3

a) Calcolare il metore Disponibile e la matrice Biogno
b) Verificare se il sistema è in uno stato sicuro (slegato e passaggi)
c) Prova 12 cattenee (0,1,02) Se viene asignato cosa succede?
d) Se richiede (1,2,0) Zz richiede (0,2,1) e richiede(1,01) verificare se si genera deadlock

TO

5. SI consideri una memoria Pagine:
    Si susuna una memoria virtuale pagina di 4096 pagine di 8 KB mappate su una memoria fisica di 2 MB.
    a) Quante sono bit dell'indirizzo legato?
b) Quante sono bit dell'indirizzo fisico?
```

---

