## Pagina 1

## Sistemi Operativi 1 – Appello d’esame del 15/01/2026

Nome e cognome ___ Matricola ___

L’obbligatorio restituisce il teste e tutti i fogli forniti, anche se non si consagga il compito.

### 1. Considerati CPU-burst time (in ms) del set di processi descritto in tabella, e considerato che l’ordine di arrivo del processi è P1, P2, P3, P4, P5.

| Processo | Burst Time | Tempo di arrivo |
| :--- | :--- | :--- |
| P1 | 5 | 0 |
| P2 | 5 | 1 |
| P3 | 4 | 3 |
| P4 | 7 | 5 |
| P5 | 2 | 7 |

a) Disegnare 1 diagramma di Gantt che illustri l'esecuzione di questi processi usando l’algoritmo di RR (quanto 4 ms). Indicare l’istante di tempo di disasciana commutazione fra un processo e l’altro.

b) Calcolare il tempo di attesa e il tempo di risposta per ciascun processo. Descrivere il procedimento usato (in estrema sintesi definire il tempo d’attesa e di risposta).

| RR - attesa | RR - risposta |
| :--- | :--- |
| P1 | |
| P2 | |
| P3 | |
| P4 | |
| P5 | |

### 2. Si consideri il seguente frammento di codice:

Si assuma che i PID assegnati siano: padre = 10, figlio = 11, nipote = 12.

a) Cosa stamperi la seguente programma?

b) Quanti processi vengono generati?

c) Descrivere brevamente Fordi di esecuzione e terminazione del processi Giustificare in estrema sintesi la risposta

```c
int var = 10;

int main()
{
    pid_t pid1, pid2;
    int status = 15;

    pid1 = fork();

    status++;
    if(pid1 != 0){
        pid1 = getpid();
        var = var + status + pid1;
    }
}
```

else {
    pid2 = fork();
    if (pid2 != 0) {
        var = var + pid1 + pid2 + status;
        wait(NULL);
    }
}

printf("var = %d \n", var);
printf("pid = %d \n", getpid());

return 0;
}

### 3. Si consideri una memoria paginata:

Si assuma uno spazio indirizi logici di 4GB su memoria fisica di 2048 frame da 2 KB

a) Quanti bit occorrono per l’indirizzo logico?

b) Quanti bit occorrono per l’indirizzo fisico?

c) Qual è la dimensione della memoria fisica?

d) Quante sono le entry di una tabella delle pagine?

Allocazione Max
T0 A B C A B C
T1 1 0 0 3 3 2
T2 1 1 2 3 3 3

b) Calcolare il vettore Disponibile e la matrice Bisogno

c) Verificare se il sistema è in uno stato sicuro (spiegare i passaggi)

d) Publ T1 richiedere subito (0,1,0) Se viene assegnato cosa succede?

### 5. Si consideri un processo che genera la seguente stringa dei riferimenti alle pagine virtuali:

4,2,3,4,5,4,1,2,1,4,3,2,5,7

a) Se il processo ha 4 frame gestiti con LRU quanti page fault vengono generati? Indicare lo stato dei frame dopo ogni accesso.

b) Qual è in working set con delta 4 al sesto accesso (incluso)?

### 6. Assumendo 3500 clindri e testina su 100

a) Data la coda di richieste 220, 2020, 150, 3200, 10, 3400 come vengono servite le richieste con shortest seek time first (SSTF)

b) Qual è la distanza in clindri percorsi della testina con l’algoritmo (SSTF)?

### 7. Splegare brevemente vantaggi e svantaggi dei metodi di allocazione dei file concatenata e indicizzazione

### 8. Descrivere il problema del trashing discutendo le strategie per prevenirlo.

---

