## Pagina 1

## Considerati CPU-burst time (in ms), priorità e tempo di arrivo del set di processi descritto in tabella

| Processo | Burst Time | Priorità | Tempo di arrivo |
| :--- | :--- | :--- | :--- |
| P1 | 6 | 3 | 0 |
| P2 | 5 | 4 | 1 |
| P3 | 2 | 1 | 2 |
| P4 | 4 | 3 | 4 |
| P5 | 5 | 1 | 5 |

Disegnare 2 diagrammi di Gantt che illustrino l'esecuzione di questi processi usando gli algoritmi di scheduling. Priorità con prelazione e SIF. Indicare l istante di tempo di clascuna commutazione fra un processo e l'alto.

Calcolare il tempo di attesa per clascun processo e per clascun algoritmo di scheduling. Descrivere il procedimento usato (in estrema sintesi definire il tempo d'attesa e come l'avete applicato per calcolare i risultati esposti)

| Priorità (prelazione) | SIF |
| :--- | :--- |
| P1 | |
| P2 | |
| P3 | |
| P4 | |
| P5 | |

Calcolare il tempo di risposta per clascun processo e per clascun algoritmo di scheduling. Descrivere il procedimento usato (in estrema sintesi definire il tempo di risposta e come l'avete applicato per calcolare i risultati esposti)

| Priorità (prelazione) | SIF |
| :--- | :--- |
| P1 | |
| P2 | |
| P3 | |
| P4 | |
| P5 | |

Tenuto cono che:

o il pid del processo padre è 11, del primo figlio 12, del secondo figlio 13, del figlio del primo figlio 14

Cosa stamperà il seguente programma? Giustificare in estrema sintesi la risposta

```c
int var1 = 40, var2 = 10;
int main()
{
    pid_t pid1, pid2;
    pid1 = fork();

    if(pid1 == 0)
        pid2 = fork();
    else
        pid2 = fork();
    var2 = var1 + pid1 + pid2;
    printf("pid = %d val = %d \n", getpid(), var);
    return 0;
}
```

Dato il frammento di programma che segue:

Cosa stamperà il programma?

A cosa serve la variabile di condizione in questa sincnizzazione?

Giustificare in estrema sintesi le risposte

pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
int c = 5, d = 5;
void* thread1(void* arg) {
    pthread_mutex_lock(&mutex);
    c = 15;
    pthread_cond_signal(&cond);
    pthread_mutex_unlock(&mutex);
    return NULL;
}

void* thread2(void* arg) {
    pthread_mutex_lock(&mutex);
    while (c == d) {
        pthread_cond_wait(&cond, &mutex);
    }
    d = 10;
    pthread_mutex_unlock(&mutex);
    return NULL;
}

int main() {
    pthread_t t1, t2;
    pthread_create(&t1, NULL, thread1, NULL);
    pthread_create(&t2, NULL, thread2, NULL);
    pthread_join(t1, NULL);
    pthread_join(t2, NULL);
    printf("c:%d d:%d", c, d);
    return 0;}
```

---

