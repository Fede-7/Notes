## Pagina 1

Nel contesto dei metodi per evitare i deadlock:
a) Dati tre thread T0, T1, T2 e tre tipi di risorsa A (6 istanze), B (7 istanze), C (7 istanze) con le seguenti matrici di Allocazione e Max:

| Allocazione | Max |
| :--- | :--- |
| A B C | A B C |
| T0 0 3 2 | 2 4 6 |
| T1 2 2 2 | 2 4 4 |
| T2 3 0 1 | 5 3 3 |

Calcolare il vettore Disponibile e la matrice Bisogno
Verificare se il sistema è in uno stato sicuro (spiegare i passaggi)
Può T0 richiedere (1, 0, 0)? Se viene assegnato lo stato rimane sicuro?

Si consideri una memoria paginata:
Si assuma una memoria virtuale paginata di 2048 indirizzo logici e pagine di 2 KB mappate su memoria fisica da 1024 frame.
Quante sono i bit dell’indirizzo logico?
Quante sono i bit dell’indirizzo fisico?
Quanti entry per una tabella delle pagine?

6. Assumendo una memoria paginata
Si assuma che il binding degli indirizzi sia supportato da una TLB con tempo di accesso di 1 ciclo di CPU e che il tempo di accesso alla memoria principale sia di 120 cicli di CPU
a) Se la miss-rate della TLB è dello 1% qual è il EAT in cicli di CPU?
b) Quale miss-rate occorre per avere un EAT minore di 160 cicli di CPU?

Si consideri un processo che genera la seguente stringa dei riferimenti alle pagine virtuali:
6, 4, 1, 5, 4, 2, 1, 4, 7, 5, 2, 4

Se il processo ha 4 frame gestiti con LRU quanti page fault vengono generati?
Quanti page fault vengono generati in più rispetto all’ottimo?
c) Spiegare brevemente la funzione degli algoritmi di sostituzione di pagina.

Spiegare brevemente vantaggi e svantaggi dei metodi di allocazione dei file contigua, concatenata e indicizzata.

9. Descrivere cos’è il thrashing in un Sistema Operativo e discutere le principali tecniche per evitarlo.

---

