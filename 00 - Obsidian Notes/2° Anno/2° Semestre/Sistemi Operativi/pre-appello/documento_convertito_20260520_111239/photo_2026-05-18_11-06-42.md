## Pagina 1

```python
void *func2(void* param)
{
    pthread_mutex_lock(&mutex);
    var2 = var1;
    pthread_mutex_unlock(&mutex);
    return 0;
}

int main()
{
    pthread t_threads[NUM_THREADS];
    int pid;
    pid = fork();
    if (pid == 0) {
        pthread_create(&threads[0], NULL, func1, NULL);
        pthread_create(&threads[1], NULL, func2, NULL);
        pthread_join(threads[0], NULL);
        pthread_join(threads[1], NULL);
    }

    printif("&d %d\n", var1, var2);
    return 0;
}

4. Nel contesto dei metodi per evitare i deadlock:
    Dai tre thread T0, T1, T2 e tre tipi di risorsa A 6 istanze, B 7 istanze, C (6 istanze) con le seguenti matrici di Allocazione e Max:
    Allocazione
    A B C
    A B C
    T0 2 1 1
    T1 2 3 1
    T2 1 1 3
    Max
    A B C
    T0 2 1 1
    T1 2 3 1
    T2 1 1 3

a) Calciare il vestito Disponibile e la matrice Biogno
b) Verificare se il sistema è in uno stato sicuro (spighe e passaggio)
c) Pud T2 ottenne (1,1,0) se viene assegnato cosa successe?
d) Se T1 richede (1,2,0), T2 richede (0,2,1) e T3 richede (1,0,1) verificare se si genera deadlock

5. Si consideri una memoria Pagina:
    Si assuma una memoria virtuale pagina di 4096 pagine di 4KB mappate su una memoria fisica di 4 MB.
    a) Quante sono b) bidi definirizzo fisico?
```

---

