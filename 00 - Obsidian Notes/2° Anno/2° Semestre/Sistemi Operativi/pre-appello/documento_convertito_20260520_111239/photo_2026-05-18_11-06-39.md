## Pagina 1

Sistem Operativo 1 – Appello di esame del 18/02/2025
Complio B

Cosa stampa il seguente programma? Giustificare in estrema sinistra la risposta int var1 = 40;
int main()
{
    pid_t pid1, pid2;
    int var2 = 50;
    pid1 = fork();
    if(pid1 == 0){
        var1 = var1 + var2;
        pid2 = fork();
    }
    else {
        pid2 = fork();
        var2 = var2 + pid1 + pid2;
    }
    printf("pid=%d var1=%d var2=%d \n", getpid(), var1, var2);
    return 0;
}

3. Dato il frammento di programma che segue:
a) Quanti processi e quanti thread genera il frammento di programma seguente?
b) Cosa stampa il programma e perché?
Giusfatore in estrema sinistra le risposte
#define NUM_THREADS 2
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
int var1 = 50, var2 = 25;

void *func1(void* param)
{
    pthread_mutex_lock(&mutex);
    while (var1 != var2)
        pthread_cond_wait(&cond, &mutex);
    var1 = 5 + var2;
    pthread_mutex_unlock(&mutex);
    return 0;
}

2. Tenuto conche:
o il pid del processo padre è 50 e quello del primo figlio è 51 e quello del secondo figlio è 53 quello del figlio del primo figlio è 52

---

